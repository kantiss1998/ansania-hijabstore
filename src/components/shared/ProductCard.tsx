'use client';

import ProductCardDefault from '../product/ProductCard';

// Export named and default versions to match all existing imports across different pages
export const ProductCard = ProductCardDefault;
export default ProductCardDefault;
